using System.Collections.ObjectModel;

namespace TransparentAccounting.Utilities
{
    public static class Constants
    {
        public static readonly int MAXIMUM_PASSWORD_ATTEMPTS = 3;
        public static readonly string CONNECTION_STRING = "server=localhost;port=3306;database=applicationdomain;user=kennesaw;password=Password123+";
    }
}